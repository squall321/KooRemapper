# Context Notes — Phase 10 적대적 리뷰 후속 조치

순차 적대적 리뷰(commit 7a596c4)가 확정한 10건에 대한 의사결정 기록이다. 각 항목은
"리뷰 원문 → 검증 단계 재평가 → 우리 결정"의 순서로 정리했다.

## 분류 기준
- 검증 단계에서 다수가 LOW로 하향됐다. 실제 사용자/개발자가 부딪히는 결함과,
  미사용 컴포넌트·설계 트레이드오프를 구분한다.
- 규칙 2(단순성)에 따라 마이그레이션이 필요한 저심각도 항목은 구현하지 않고 표면화한다.

## 지금 고치는 것 (명확한 수정)
1. **로그인 타이밍 사이드채널 (medium, services.py)** — 계정 존재/활성 여부가 응답
   지연으로 새어 이메일 열거가 가능하다. 표준 완화책인 더미 해시 상수시간 검증을 적용한다.
2. **회원가입/관리자 생성 비밀번호·이메일 검증 불일치 (low, schemas.py)** — PasswordChange는
   공백 비밀번호를 막지만 signup/admin-create는 안 막는다. 이메일도 형식 검증이 없다.
   공통 검증 헬퍼로 일관화한다.
3. **마지막 관리자 잠금 방지 부재 (low, admin/routes.py)** — self-guard만 있어 두 관리자가
   서로 강등하는 TOCTOU 경합 시 관리자 0명이 될 수 있다. count 기반 가드를 추가한다(저비용).
4. **change_password의 죽은 is_active 가드 (low, auth/routes.py)** — get_current_user가
   이미 비활성 사용자를 401로 막으므로 도달 불가. 우리가 추가한 줄이라 제거한다(규칙 3).
5. **Pagination 빈 페이지 범위 "0–20" (low, Pagination.tsx)** — `count===0` 이면 무조건
   null 반환으로 수정한다(사소·내 코드).
6. **SystemPage CopyBtn 타이머 미정리 (low)** — 재클릭 시 이전 타이머 미해제. 스케줄 전 clear.
7. **AccountPage 동일 메시지 재표시 시 타이머 미갱신 (low)** — nonce 트리거로 자동소멸 재무장.
8. **ComparePanel 반올림 후 A==B인데 Δ≠0 (low)** — 표시값(포맷 결과) 기준으로 같음을 판정.

## 구현하지 않고 표면화하는 것 (설계 트레이드오프)
- **비밀번호 재설정 시 기존 JWT 무효화 안 됨 (#3, low)** — 무상태 JWT의 알려진 트레이드오프다.
  제대로 고치려면 User에 token_version 컬럼 + 마이그레이션 + 발급/검증 변경이 필요하다.
  완화 요소가 이미 있다(JWT TTL 12h, MCP용 PAT는 revoked_at으로 폐기 가능). 저심각도에
  비해 비용이 커서 규칙 2에 따라 보류하고 사용자에게 결정을 위임한다.
- **Pagination 꽉 찬 마지막 페이지 off-by-one (#7, low)** — total/hasMore 입력이 없으면
  꽉 찬 중간 페이지와 마지막 페이지를 구분할 수 없다. 현재 어디에도 연결되지 않은(dead)
  컴포넌트라, 미사용 상태에서 API를 추가하는 것은 투기적이다(규칙 2). 연결 시점에 total을
  넘기도록 컴포넌트에 주석으로 한계를 명시하고 보류한다.

## 검증 방식
- 백엔드: pytest 전체 통과 + 회원가입 공백/이메일 검증 테스트 추가.
- 프론트엔드: tsc + vite 빌드 통과.
- 라이브: api 재시작 후 회원가입 검증 동작 확인.

---

# 2026-09-23 — 인클루드·설정 파일 저장 정합성 (include-integrity)

## 왜 하는가 — 실측으로 드러난 사실 셋

사용자 질문("인클루드된 별도 파일이나 설정 파일도 저장되냐")을 코드·dev DB·디스크·바이너리로 확인한 결과다.

1. **리더는 `*INCLUDE` 를 따라가지 않는다.** `KooRemapper info` 로 확인: 인클루드 파일이 있어도
   없어도 노드 수가 같다(2/2). 인클루드된 내용은 모델에 들어오지 않는다.
2. **라이터는 `*INCLUDE` 줄을 보존한다.** `indent` 로 확인: 입력의 `*INCLUDE sub/extra.k` 가
   출력 덱 4–5행에 그대로 남고, op 자신의 `*INCLUDE <output>.dynain` 이 뒤에 붙는다.
3. **업로드는 경로를 평평하게 만든다.** `storage.safe_filename` 이 `Path(name).name` 으로 디렉터리를
   전부 떼어낸다 — `sub/part.k` → `part.k`.

**1+2+3 이 합쳐지면 사고가 된다.** 하위 폴더 인클루드를 가진 덱을 올려 op 을 돌리면, 산출물은
`sub/part.k` 를 가리키는데 세션에는 `part.k` 밖에 없다. 내려받아 LS-DYNA 에 넣으면 — LS-DYNA 는
인클루드를 따라가므로 — 실패하거나 조용히 파트가 빠진다. 도구는 아무 말도 하지 않는다.

## 설계 결정

**③ 하위 경로 보존이 정합성의 본체다**(앞서 "손이 제일 많이 간다" 고 적었지만 우선순위는 반대로 제일 높다).
②는 깨졌을 때 알려 주는 관문, ①은 재현성 위생이다.

- **경로 보존** — `safe_filename`(평탄화)은 그대로 두고 `safe_relpath`를 새로 만든다. 상대 경로만 허용하고
  `..`·절대경로·드라이브 문자를 제거하며 구성요소마다 기존 문자 규칙을 적용한다. 쓰기 직전에 목적지가
  세션 디렉터리 안인지 재확인한다(읽기 쪽은 `abs_path`가 이미 막는다 — 방어를 두 겹으로).
  `session_files.filename` 은 이미 하위 경로를 담을 수 있다(워커가 중첩 산출물을 그렇게 등록한다).
- **정합성 검사** — `meta.includes` 를 세션 파일 목록과 대조한다. 업로드 응답에 경고로 싣고(막지 않음),
  잡 생성 때 422 로 막되 **명시적 우회 플래그**를 둔다. 리더가 인클루드를 안 읽으므로 "op 은 성공하는데
  산출물이 깨지는" 형태라, 실행 전에 막는 것이 유일하게 효과적인 지점이다.
- **설정 파일 등록** — `build_command` 가 쓴 파일들을 `kind="generated"` 로 **명시 등록**한다.
  스냅샷 순서를 바꾸는 방식(=산출물로 잡히게)은 의미가 틀리다 — 그것은 바이너리의 산출물이 아니라
  플랫폼이 만든 입력이다. `inspect_kfile` 은 돌리지 않는다(yaml 이라 `info` 가 실패한다).

## 확인 방식
- 경로 트래버설: `..`·절대·드라이브·심링크·긴 경로·빈 구성요소를 단위 시험으로 막는다.
- 정합성: 하위 폴더 인클루드 덱을 올려 경고가 나오는지, 빠뜨리면 잡이 422 로 막히는지.
- 설정: 잡을 돌린 뒤 파일 목록에 `config.yaml` 이 `generated` 로 뜨고 내려받아지는지.


---

# 2026-09-24 — 덱 계약 2차 계획을 세우며 뒤집힌 전제 다섯

실사용 박스 답신(`docs/requests/deck-contract-followup-2026-09-24.md`)과 5갈래 실측으로 드러났다.
**다음 사람이 이 다섯을 근거로 삼지 않게 적어 둔다.**

1. **`9000001` 은 예약 번호가 아니다.** `hfdamp` 이 `*SET_PART` 최대 +1 로 발행한 값이다
   (`src/commands/hfdamp.cpp` maxSetId). 그래서 DF-07 은 "대역표를 갖는다" 가 아니라
   **"발행자들이 그 표를 본다"** 여야 뜻이 있다. 발행자는 8개 파일이고 전부 `*INCLUDE` 를 안 본다.

2. **DF-07 은 SET id 축으로 좁혀졌다.** StepForge `docs/ID_BANDS.md` §5 가 PID 축을 사내 관례
   공식으로 걷어냈다. 즉 **"대역표 값을 기다려 막힌 항목" 은 이제 없다** — SET 축은 값 없이 진행된다.

3. **`requires_gmsh` 는 `meshfix` 하나뿐이고 `api.sif` 안에 gmsh 가 없다.** 컨테이너에서
   `which gmsh` 가 잡히는 것은 홈 바인드로 호스트의 깨진 파이썬 래퍼가 보이는 것이다
   (`--no-home --cleanenv` 면 없다). "컨테이너 안에서 회귀를 돌린다" 로는 안 풀리고,
   해법은 `KOOREMAPPER_GMSH` 를 `dist/gmsh/gmsh` 로 고정하는 것이다.

4. ⚠ **매트릭스의 skip 은 통과가 아니다.** `test_newline_matrix.py` 가 의존성 실패·예제 부재·
   바이트 동일을 한 칸에 몰아넣고 `ALL OK` 를 찍는다. 그래서 `meshfix` 를 놓쳤고, 전수로 재니
   **개행을 잃는 op 이 11개**였다(보고는 "19개 보존 · 깨짐 0" 이었다). 덱을 쓰는 op 은 42개인데
   판정된 것은 19개였다. **"전부 통과" 가 "전부 돌았다" 는 아니다.**

5. ⚠ **호스트 cmake 로 빌드하면 컨테이너에서 안 돈다.** 09-24 08:09 에 다른 세션이 그렇게 덮어써
   `platform/backend/bin/KooRemapper` 가 **GLIBC_2.38** 을 요구하고 있었다(`cli.sif` 는 2.36).
   `scripts/build_linux_compat.sh` 로 되돌렸다(2.34). `dist-to-drive.sh` 는 glibc 를 BUILD_INFO 에
   **적기만 하고 거절하지 않는다** — 그대로 게시하면 실행 자체가 안 되는 바이너리가 나간다.

그리고 **게시본이 15커밋 뒤에 멈춰 있다**(Drive latest = `cb4e208`, 09-21). 09-24 덱 계약 수정
9건이 실사용(pyKooCAE REMAP 체인 = SIF 안 바이너리)에 하나도 안 갔다.
