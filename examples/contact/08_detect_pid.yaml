# 08_detect_pid.yaml — 명시적 PID 간 접촉 자동 감지
# PID 1 ↔ PID 2 사이의 맞닿는 세그먼트를 검출하고 접촉을 자동 생성한다.
model:  model.k
output: 08_detect_pid_result.k

contacts:
  - action: detect
    slave:  { pid: 1 }
    master: { pid: 2 }
    tolerance: 0.1
    auto_create: true
    contact_type: auto
    friction: 0.20
