# PID=9 하단면(z-)에 spider RBE3 생성
# → 1개 centroid 노드 + CONSTRAINED_INTERPOLATION

model: ../load/mesh.k
output: mesh_rbe_spider.k

rbe:
  - part: 9
    select: direction
    direction: [0, 0, -1]
    angle: 45.0
    type: rbe3
    mode: spider
