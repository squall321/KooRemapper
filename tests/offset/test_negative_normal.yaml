# Test: -normal direction offset (bug fix validation)
# Purpose: Verify that -normal offset no longer produces negative Jacobian

base_model: ../../examples/arc30/arc30_flat.k
output: test_negative_normal_result

operations:
  - type: offset
    source_pid: 1
    element_type: solid
    thickness: 1.0
    num_layers: 1
    offset_direction: -normal      # Inward offset (previously buggy)
    connection_mode: tied
    new_pid: 10
    part_title: "Negative_Normal_Test"
    material_card: |
      *MAT_ELASTIC
      $#     mid        ro         e        pr
           @MID@       2.0     12000       0.25
