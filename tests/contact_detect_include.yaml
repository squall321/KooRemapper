model: contact_test_model.k
output: contact_detect_include_result.k

contacts:
  - action: detect
    include: [Left]
    tolerance: 0.1
    auto_create: true
    contact_type: tied
    title_prefix: Tied
